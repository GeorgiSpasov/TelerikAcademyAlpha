{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            //"protocol": "inspector", // For debugging with node.js < 8
            "request": "launch",
            "name": "Launch Program",
            "program": "${file}"
        }
    ]
}